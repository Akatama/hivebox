[project]
name = "hivebox"
version = "0.0.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.0",
    "requests>=2.33.1",
    "toml>=0.10.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
